#include <stdio.h>
#define rep(x, y, z) for (int x = y; x < z; x++)

int main(void)
{

    int a, b, c, d;
    int add,sub;
    scanf("%d%d%d%d", &a, &b, &c, &d);

    printf("%d", (a + b) * (c - d));
    puts("Takahashi");

    return 0;
}