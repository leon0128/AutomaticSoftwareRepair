#include<stdio.h>
#include<string.h>
int main(void) {
	int a, b, c, d;
	int ans = 0;
	scanf("%d%d%d%d", &a, &b, &c, &d);

	ans = (a + b) * (c - d);
	printf("%d\n", ans);


	printf("Takahashi");
	return 0;
}