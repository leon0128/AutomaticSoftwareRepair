#include <stdio.h>
int main(int argc, char const *argv[])
{
    int a, b, c, d, e;
    scanf("%d %d %d %d", &a, &b, &c, &d);

    e = (a + b) * (c -d);

    printf("%d\nTakahashi", e);
    
    return 0;
}
