#include<string.h>
#include <stdio.h>

int main()
{
    char str[110],st[2],s;
    int i,x,n,m;
    scanf("%s",str);
    x=strlen(str);
    n=(x/2);
    for(i=n;i<=n;i++)
    {
        printf("%c",str[i]);
    }
    return 0;
}
